kind = "web"
    previewPath = "/"
    title = "PRE-Atendimento V8"
    version = "1.0.0"
    id = "artifacts/api-server"
    router = "path"

    [[integratedSkills]]
    name = "react-vite"
    version = "1.0.0"

    [[services]]
    name = "web"
    paths = [ "/" ]
    localPort = 3001

    [services.development]
    run = "pnpm run dev"

    [services.production]
    run = "pnpm start"

    [services.env]
    PORT = "3001"
